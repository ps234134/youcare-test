{{-- <!DOCTYPE html>
<html lang="nl">

<head>
    <title>Gebruiker details</title>
    <meta charset="UTF-8">
    @vite('resources/css/app.css')
</head>

<body>
    <div class="container mx-auto mt-10">
        <h1 class="text-3xl font-bold mb-5">Gebruiker details</h1>
        <div>
            <p><strong>Naam:</strong> {{ $user->name }}</p>
            <p><strong>Email:</strong> {{ $user->email }}</p>
            <p><strong>Rollen:</strong>
                @foreach ($user->roles as $role)
                    {{ $role->name }}
                @endforeach
            </p>
        </div>
        <div class="mt-5">
            <a href="{{ route('users.index') }}"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Terug naar
                gebruikerslijst</a>
        </div>
    </div>
</body>

</html> --}}
